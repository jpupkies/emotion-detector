# Emotion Detector

This project is an AI-based application that detects emotions from text using the Watson NLP library. 

It is packaged as a Python module (`EmotionDetection`) and deployed via Flask for web access. 

The application supports unit testing, error handling, and static code analysis.
